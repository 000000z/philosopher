/*

init_philo()
{
    while (i < data->nbr_philo) // la ca va boucler pour que chaque philo initialise les donnes
    {
        init le mutex de la right_fork;
        init le mutex du nbr de repas manger 
        philo[i].id = 
    }
}

philo = malloc 

int main()
{
    check si ya 5 ou 6 args
        sinn salam
    init_data(av, arc, t_data);
    malloc le nbr de philo dans un tableau de struct en taille de t_philo; --> permet de creer une carte d'idendite pour chq philo avec des donnes qui leur sont propres
    init_philo();

}


1 - init les mutex global (commun a tous)
2 - donner identite au philo (boucler sur nbr de philo et donner les data avec &philo[i]) 
3 - cree tes threads dans une boucle puis pthread_join dans une autre
4 - faire une petite fonction qui print les messages juste avec un mutex et un printf
5 - creer une routine avec : manger, thinking et sleep ou juste tu print les message "id is sleeping" "id is thinking" "id is eating" ici id  = le nbr du philo 





philo->data->time_to_die



*/